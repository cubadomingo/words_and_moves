<div class="container">
  <br />
  <div class="row">
    <div class="col-md-6">
      <div class="panel panel-danger">
      <div class="panel-heading">Popular</div>
        <% for region in @regions %>
          <h3><%= link_to region.name, region_path(region.slug) %></h3>
        <% end %>
      </div>
    </div>
    <div class="col-md-6">
      <div class="panel panel-danger">
        <div class="panel-heading">Recently Added</div>
        <% for region in @regions %>
          <h3><%= link_to region.name, region_path(region.slug) %></h3>
        <% end %>
      </div>
    </div>
  </div>
</div>
