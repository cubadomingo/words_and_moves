<% if signed_in? && item.like?(current_user) %>
  <%= link_to "Unsubscribe", unsubscribe_path(item_id: item.id, item_class: item.class), method: :delete, id: "unlike" do %>
  <% end %>
  <!-- If user is not signed in and/or has not subscribed, it shows a subscribe button -->
<% else %>
  <%= link_to "Subscribe", subscribe_path(region), method: :post, id: "subscribe" %>
<% end %>
