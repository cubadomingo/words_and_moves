  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="col-xs-6">
          <div class="logo-margin">
            <%= link_to image_tag('logo-pink.png', class: "logo"), root_path %>
          </div>
      </div>
      <div class="col-xs-6 text-right">
          <span><%= link_to 'Explore', regions_path, class: "btn btn-primary nav-button" %></span>
          <% if user_signed_in? %>
            <span><%= link_to 'My Feed', feed_path, class: "btn btn-primary sign-button" %></span>
            <span><%= link_to 'My Account', edit_user_registration_path, class: "btn btn-primary sign-button" %></span>
            <span><%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-primary sign-button", form_class: "sign-out"%></span>
          <% else %>
            <%= link_to "Sign in or Sign up", new_user_session_path, class: "btn btn-primary sign-button"%>
          <% end %>
      </div>
    </div>
  </nav>
